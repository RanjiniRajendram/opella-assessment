# Terraform Workspace

This folder contains the full Terraform configuration for the challenge. It includes:

* `modules/vnet` - reusable network module
* `environments/dev`, `environments/qa`, `environments/prod` - environment-specific deployments
* `backend_storage` - shared backend configuration (state storage)
* `policy` - governance definitions

Below is an aggregated view of the variables and outputs that matter at the workspace level. The details are generated by [terraform-docs](https://terraform-docs.io/) and injected automatically.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->

Notes:
* Documentation is produced for the entire workspace; individual module or environment READMEs are not used.
* Run `terraform-docs markdown table --output-file=terraform/README.md --output-mode=inject terraform` to regenerate.
